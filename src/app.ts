// const express = require("express"); //this is the actual Node import syntax
import express from "express"; //after installing the Node/Express Types, module imports work with TS support

const app = express();

app.listen(3000);
